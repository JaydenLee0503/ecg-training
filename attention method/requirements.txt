# Separate optional dependency: do not alter the frozen VMD/WST environment.
# CPU verification used the official CPU index and torch 2.6.0+cpu.
# numpy/scipy versions match the existing project environment.
numpy==2.5.2
scipy==1.18.1
torch==2.6.0
