# ECG arrhythmia notebooks — verified working set (Python 3.12.3, Ubuntu 24.04 / WSL2)
numpy==2.5.2
scipy==1.18.1
matplotlib==3.11.1
pandas==3.0.5
scikit-learn==1.9.0
joblib==1.5.3
tqdm==4.70.0

# vmdpy is NOT required: ecgvmd/vmd.py has its own solver, verified against
# vmdpy to 2e-16. Keep the pin only if you want to re-run that cross-check.
# vmdpy==0.2

# notebook runtime
jupyterlab==4.6.3
ipykernel==7.3.0
ipywidgets==8.1.9

# --- quantum stage (ecgvmd/quantum.py) ------------------------------------
# Imported lazily, so the classical pipeline runs without these installed.
# lightning.qubit is the C++ statevector simulator; 12 qubits is trivial for it.
# Pin exactly: PennyLane's device and qnn APIs churn between minor releases.
pennylane==0.45.1
pennylane-lightning==0.45.0
